<div class="container">

    <h1 class="mb-3">Platforms streaming {{title}}</h1>
    <hr>
    <img src="{{poster}}" alt="Program Image">
    <hr>
    <div class="row">

        {{#each sources}}
        <div class="col-md-4">
            <h5 class="mb-2"> {{name}} </h5>

            {{#if price}}

            <p> {{format}}</p>
            <p>{{type}} - ${{price}}</p>

            {{else}}

            <p> {{format}} - {{type}} </p>

            {{/if}}

        </div>
        {{/each}}
    </div>

    <a href="/" class="btn btn-dark mb-5">Back to HOME</a>
</div>